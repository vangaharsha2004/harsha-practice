#include<stdio.h>
int main(){
	printf("the size of the integer %ld\n",sizeof(int));
	printf("the size of the character %ld\n",sizeof(char));
	printf("the size of the float %ld\n",sizeof(float));
	printf("the size of the integer %ld\n",sizeof(double));
	return 0;
}
